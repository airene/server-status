# 侦听地址, ipv6 使用 [::]:9394
grpc_addr = "0.0.0.0:9879"
http_addr = "0.0.0.0:9878"

# disabled = true 单机禁用，跟删除这条配置的效果一样
hosts = [
    { name = "h1", password = "p1", location = "🏠" },
    { name = "h2", password = "p2", location = "🏢" },
]

# server_url = "https://127.0.0.1:/report"
# stat_client 默认安装的路径
workspace = "/opt/server-status"
